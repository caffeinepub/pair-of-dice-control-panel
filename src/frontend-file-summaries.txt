{
  "frontend/src/App.tsx": "Root application component that wraps the control panel screen with ThemeProvider and Toaster for notifications.",
  "frontend/src/screens/ControlPanelScreen.tsx": "Main control panel screen layout with ControlLayoutProvider wrapping workspace, inspector (edit mode only), and signals panels; ImportExportPanel is now hidden in runtime mode to prevent layout mutations.",
  "frontend/src/types/controlPanel.ts": "TypeScript type definitions for control configurations including button, toggle, slider, and radio group types with their specific properties; now includes sliderIsVertical field for slider orientation alongside radioGroupIsVertical for radio orientation.",
  "frontend/src/hooks/useControlLayout.ts": "Exports the ControlLayoutContext and useControlLayout hook for accessing shared control layout state; the context interface defines all control CRUD operations including support for sliderIsVertical in createControlWithConfig, selection state, and validation methods.",
  "frontend/src/hooks/usePanelMode.ts": "Custom hook managing edit/runtime mode toggle with safe localStorage persistence that falls back to 'edit' mode when storage is unavailable or blocked.",
  "frontend/src/hooks/useSignalEmitter.ts": "Custom hook for emitting binary signal events to the backend with control name (string or null) included in the event payload, with proper typing and query invalidation on success.",
  "frontend/src/hooks/useRecentSignals.ts": "Custom hook for fetching recent signal events from the backend with polling support and staleTime set to 0 to ensure fresh fetches after invalidation.",
  "frontend/src/lib/controlDefaults.ts": "Utility providing default configuration values for each control type (button, toggle, slider, radio); slider defaults now include sliderIsVertical set to false (horizontal), and radio defaults include radioGroupIsVertical set to true.",
  "frontend/src/lib/binaryCode.ts": "Utilities for validating binary code strings (0/1 only) and generating deterministic default binary codes from string seeds.",
  "frontend/src/lib/layoutSerialization.ts": "Functions for validating, exporting, and importing control panel layouts as JSON with comprehensive error reporting; now validates optional sliderIsVertical and radioGroupIsVertical boolean fields for their respective control types.",
  "frontend/src/lib/download.ts": "Utility function to trigger JSON file downloads in the browser.",
  "frontend/src/lib/utils.ts": "Utility function for merging Tailwind CSS classes using clsx and tailwind-merge.",
  "frontend/src/components/control-panel/Workspace.tsx": "Canvas component rendering all controls at their configured positions with selection support; correctly passes isDraggable based on panel mode to prevent dragging in runtime.",
  "frontend/src/components/control-panel/DragController.tsx": "Wrapper component enabling drag-to-reposition functionality for controls in edit mode; cursor styling now changes based on isDraggable (move cursor in edit, default in runtime) and selection outline only shows in edit mode.",
  "frontend/src/components/control-panel/ControlRenderer.tsx": "Renders individual controls (button, toggle, slider, radio) with pointer event handlers; in runtime mode, interactions use local React state instead of mutating control configuration, while still emitting events with control name (or null) and logging button presses to console.",
  "frontend/src/components/control-panel/InspectorPanel.tsx": "Property editor panel for selected control with fields for id, label, type, position, size, color, binary code, and type-specific settings; sliders now include orientation selector (Horizontal/Vertical) and radio groups include orientation selector (Vertical/Horizontal), with fixed property spread order to avoid TypeScript errors.",
  "frontend/src/components/control-panel/RecentSignalsPanel.tsx": "Panel displaying recent emitted binary signals with timestamps, binary codes, control names (labels), and metadata, with manual refresh capability.",
  "frontend/src/components/control-panel/ImportExportPanel.tsx": "UI for exporting current layout to JSON and importing layouts with validation error display; all import/export actions are now disabled in runtime mode to prevent layout mutations.",
  "frontend/src/components/control-panel/ModeToggle.tsx": "Button component for toggling between edit and runtime modes with visual indication.",
  "frontend/src/components/control-panel/CreateControlButton.tsx": "Thin wrapper component that delegates to SharedAddControlButton to maintain the control panel header's Add Control button behavior.",
  "frontend/src/index.css": "Global styles with OKLCH color system for dark theme, custom scrollbar styling, control-specific utility classes including vertical slider styling (.slider-vertical), and fullscreen styling for the control workspace region to ensure proper viewport filling and background behavior.",
  "frontend/tailwind.config.js": "Tailwind configuration extending theme with custom border radius (xl) and maintaining OKLCH color system mappings.",
  "frontend/src/components/control-panel/AddControlModal.tsx": "Modal dialog for configuring new controls with validation; now includes orientation selector for both slider controls (Horizontal/Vertical) and radio groups, and only closes on successful creation.",
  "frontend/src/components/control-panel/ControlLayoutProvider.tsx": "React Context provider that manages control layout state, backend synchronization, and CRUD operations; now handles serialization/deserialization of both sliderIsVertical and radioGroupIsVertical fields with backward compatibility (sliders default to horizontal, radios default to vertical).",
  "frontend/src/hooks/useFullscreen.ts": "React hook that manages Fullscreen API interactions for a provided element ref using safe browser utilities to prevent crashes when document/fullscreen APIs are unavailable during initial render.",
  "frontend/src/components/control-panel/FullscreenToggleButton.tsx": "Button component that toggles fullscreen mode for the workspace region using the useFullscreen hook and displays appropriate icons/labels (Maximize/Minimize) with error handling via toast notifications; now accepts workspaceRef instead of containerRef.",
  "frontend/index.html": "HTML entry point with dark theme class applied to html element for consistent first-paint theming in both new-tab and embedded contexts.",
  "frontend/index.css": "Root-level stylesheet now mirroring frontend/src/index.css exactly, including dark theme OKLCH color tokens, control-panel-specific styles (control-lit, fullscreen workspace region), and custom scrollbar styling to ensure identical rendering in new-tab and in-Caffeine contexts.",
  "frontend/src/lib/safeBrowser.ts": "Utility module providing safe wrappers for browser-only APIs (localStorage, window.location, document, fullscreen detection) that handle unavailable or blocked APIs gracefully without throwing during initial render.",
  "frontend/src/components/control-panel/SharedAddControlButton.tsx": "Shared Add Control button component that encapsulates guard checks (initialized + edit mode + not loading), disabled tooltip messaging with reliable hover support via wrapper span with tabIndex and aria-disabled, loading spinner icon selection, and modal open/close behavior without auto-close logic so the modal stays open until user closes it or creation succeeds."
}