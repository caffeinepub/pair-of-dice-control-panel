{
  "frontend/src/App.tsx": "Root application component that wraps the control panel screen with ThemeProvider and Toaster for notifications.",
  "frontend/src/screens/ControlPanelScreen.tsx": "Main control panel screen layout with ControlLayoutProvider wrapping workspace and sidebars; left sidebar (edit mode) now wraps InspectorPanel and UserExtensionsSection in a flex column container with proper spacing to prevent overlap and ensure scrollability.",
  "frontend/src/types/controlPanel.ts": "TypeScript type definitions for control configurations with RadioOption now using decimalCode (number 1-16) instead of binaryCode; maintains all other control type properties and decimal code fields.",
  "frontend/src/hooks/useControlLayout.ts": "Exports the ControlLayoutContext and useControlLayout hook for accessing shared control layout state; createControlWithConfig now accepts a flexible Partial<ControlConfig> type to support all control configurations including decimal codes.",
  "frontend/src/hooks/usePanelMode.ts": "Custom hook managing edit/interact mode toggle with module-level shared state using useSyncExternalStore for immediate cross-component synchronization, safe localStorage persistence, and migration of old \"runtime\" values to \"interact\".",
  "frontend/src/hooks/useSignalEmitter.ts": "Custom hook for emitting signal events to the backend; now correctly passes commandStr parameter (7th argument) to emitButtonEvent for all control types with comprehensive console logging for debugging signal transmission.",
  "frontend/src/hooks/useRecentSignals.ts": "Custom hook for fetching recent signal events from the backend with polling support at 2-second intervals; now includes error state in return value and comprehensive console logging for debugging event fetching.",
  "frontend/src/lib/controlDefaults.ts": "Provides default configuration values for each control type with decimal codes; radio options now use decimalCode (1-16) instead of binaryCode, and generateDualCodesForControl generates appropriate decimal code pairs for all control types.",
  "frontend/src/lib/binaryCode.ts": "Utilities for validating binary code strings (must be exactly 4 characters of only 0/1) and generating deterministic 4-bit default binary codes from string seeds.",
  "frontend/src/lib/layoutSerialization.ts": "Functions for validating, exporting, and importing control panel layouts as JSON with comprehensive error reporting; validates all decimal codes are in range 1-16 for all control types.",
  "frontend/src/lib/download.ts": "Utility function to trigger JSON file downloads in the browser.",
  "frontend/src/lib/utils.ts": "Utility function for merging Tailwind CSS classes using clsx and tailwind-merge.",
  "frontend/src/components/control-panel/Workspace.tsx": "Canvas component rendering all controls at their configured positions with selection support; automatically clears selection when switching to interact mode to prevent edit interactions.",
  "frontend/src/components/control-panel/DragController.tsx": "Wrapper component enabling drag-to-reposition functionality for controls in edit mode; blocks all selection and dragging in interact mode while keeping child controls clickable/pressable.",
  "frontend/src/components/control-panel/ControlRenderer.tsx": "Renders individual controls with pointer and keyboard event handlers; button interactions now send HTTP POST requests with decimal code and \"on\"/\"off\" state instead of executing gpioset commands, while maintaining backend event logging with command strings.",
  "frontend/src/components/control-panel/InspectorPanel.tsx": "Inspector panel for selected control with decimal code editing fields (1-16); now uses a simple space-y-4 wrapper instead of flex-shrink-0 Card to prevent layout conflicts, with ImportExportPanel rendered at the bottom after a separator in both selected and non-selected states.",
  "frontend/src/components/control-panel/RecentSignalsPanel.tsx": "Panel displaying recent emitted signals with timestamps, decimal codes, control names, metadata, and gpioset command strings; now displays the commandStr field in a monospace font with background highlighting for easy identification.",
  "frontend/src/components/control-panel/ImportExportPanel.tsx": "UI for exporting current layout to JSON and importing layouts with validation error display; all import/export actions are disabled in interact mode, uses correct downloadJSON function name.",
  "frontend/src/components/control-panel/ModeToggle.tsx": "Button component for toggling between Edit Mode and Interact Mode with visual indication using Edit and Play icons.",
  "frontend/src/components/control-panel/CreateControlButton.tsx": "Button component for adding new controls that opens AddControlModal; disabled in interact mode or when not initialized, with tooltip explaining the disabled state and loading spinner icon when loading.",
  "frontend/src/index.css": "Global styles with OKLCH color system for dark theme, custom scrollbar styling, control-specific utility classes including vertical slider styling (.slider-vertical), and fullscreen styling for the control workspace region to ensure proper viewport filling and background behavior.",
  "frontend/tailwind.config.js": "Tailwind configuration extending theme with custom border radius (xl) and maintaining OKLCH color system mappings.",
  "frontend/src/components/control-panel/AddControlModal.tsx": "Modal dialog for configuring new controls; radio options now use decimal code input fields (1-16) instead of binary codes, with automatic sequential code assignment when adding new options.",
  "frontend/src/components/control-panel/ControlLayoutProvider.tsx": "React Context provider managing control layout state and backend synchronization; radio options are now deserialized with decimal codes 1-16 based on their array index position, and serialized as label-only arrays to the backend.",
  "frontend/src/hooks/useFullscreen.ts": "React hook that manages Fullscreen API interactions for a provided element ref with improved state tracking that syncs immediately on mount and after fullscreen changes to prevent stale state across mode switches.",
  "frontend/src/components/control-panel/FullscreenToggleButton.tsx": "Button component that accepts externally-provided fullscreen state and toggle handler, displaying appropriate icons/labels (Maximize/Minimize) with error handling via toast notifications.",
  "frontend/index.html": "HTML entry point with dark theme class applied to html element for consistent first-paint theming in both new-tab and embedded contexts.",
  "frontend/index.css": "Root-level stylesheet now mirroring frontend/src/index.css exactly, including dark theme OKLCH color tokens, control-panel-specific styles (control-lit, fullscreen workspace region), and custom scrollbar styling to ensure identical rendering in new-tab and in-Caffeine contexts.",
  "frontend/src/lib/safeBrowser.ts": "Utility module providing safe wrappers for browser-only APIs (localStorage, window.location, document, fullscreen detection) that handle unavailable or blocked APIs gracefully without throwing during initial render.",
  "frontend/src/components/control-panel/SharedAddControlButton.tsx": "Shared Add Control button component that encapsulates guard checks (initialized + edit mode + not loading), disabled tooltip messaging with reliable hover support via wrapper span with tabIndex and aria-disabled, loading spinner icon selection, and modal open/close behavior without auto-close logic so the modal stays open until user closes it or creation succeeds.",
  "frontend/src/components/control-panel/UserExtensionsSection.tsx": "New scaffold component with clearly marked BEGIN/END USER CODE comment delimiters and a placeholder UI card for user custom functionality; includes helpful TODO comments with examples of available hooks and components.",
  "frontend/src/lib/gpiosetCommands.ts": "Utility functions for generating gpioset commands from decimal codes and a new executeGpiosetCommand function that logs commands in the format \"gpioset -c gpiochip0 <controlId>=<value>\" for all control interactions.",
  "frontend/scripts/rpi_event_runner.sh": "Executable Bash script that polls the backend canister's getRecentEvents method every 2 seconds, parses events, executes whitelisted GPIO commands locally on Raspberry Pi with deduplication via state file, supports DRY_RUN mode, includes comprehensive --help output documenting all environment variables and prerequisites, and logs all operations with timestamps.",
  "frontend/docs/rpi-bash-runner.md": "Complete documentation for Raspberry Pi GPIO tools including event format examples for all control types, GPIO pin mapping table, testing instructions, manual execution examples with all environment variables, comprehensive systemd service setup section with step-by-step instructions for auto-start at boot, troubleshooting for both manual and systemd usage, security considerations, and advanced configuration options.",
  "frontend/scripts/rpi_pin_test.sh": "Standalone Bash script for verifying GPIO pins (26, 6, 22, 4) on Raspberry Pi with DRY_RUN mode, interactive step-through mode with prompts and observation instructions, non-interactive auto-cycling mode with configurable delays, dependency validation for gpioset with install hints, and safety behavior that sets each pin HIGH briefly then returns to LOW with cleanup on exit.",
  "frontend/scripts/package_linux_bundle.sh": "Bash script that builds frontend and backend, validates all required artifacts including backend WASM, and packages everything into a distributable .tar.gz archive including new systemd configuration examples in scripts/systemd/ subdirectory with improved README mentioning optional boot setup.",
  "frontend/scripts/linux_bundle_run.sh": "Launcher script for the Linux bundle that validates bundle contents, starts or reuses local replica, deploys backend canister, and provides comprehensive access instructions including Raspberry Pi GPIO setup steps, systemd service setup reference, and clear copy-paste commands for serving frontend, running event runner, and controlling the replica.",
  "frontend/docs/linux-local-test-bundle.md": "Complete guide for downloading, extracting, and running the Linux test bundle including prerequisites, detailed bundle contents listing with new systemd subdirectory, launcher script usage with all commands, Raspberry Pi GPIO setup with manual and systemd auto-start instructions, comprehensive troubleshooting section with remediation steps for common failures, and advanced configuration options for production deployment.",
  "frontend/scripts/lib/script_diagnostics.sh": "Shared bash helper library that provides a run_step function to execute commands with comprehensive error diagnostics including step name, exact command, exit code, and last 100 lines of log output on failure while keeping successful runs concise.",
  "frontend/src/lib/gpioHttp.ts": "Utility function that sends HTTP POST requests to http://localhost:3000/gpio with JSON payload containing decimal code (1-16) and state value (\"on\" or \"off\").",
  "frontend/src/lib/buttonCode.ts": "Utilities for validating decimal codes (1-16), generating deterministic decimal codes from string seeds, and converting between decimal and binary formats for GPIO operations.",
  "frontend/scripts/rpi_event_runner.service.example": "Systemd unit file template for running the Raspberry Pi event runner as a service at boot, with placeholders for WorkingDirectory, EnvironmentFile, and ExecStart paths that users must update to match their bundle location.",
  "frontend/scripts/rpi_event_runner.env.example": "Example environment configuration file documenting all required (CANISTER_ID, NETWORK) and optional (DRY_RUN, POLL_INTERVAL, STATE_FILE, ALLOWED_PREFIX) environment variables for the Raspberry Pi event runner with inline comments explaining each setting."
}