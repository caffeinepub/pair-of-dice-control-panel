# GPIO Control Panel Event Runner Configuration
# Copy this file to rpi_event_runner.env and edit the values below

# REQUIRED: Backend canister ID (get this from the run.sh output or dfx canister id backend)
CANISTER_ID=your-canister-id-here

# REQUIRED: Network to connect to
# Options: "local" (for local replica) or "ic" (for Internet Computer mainnet)
NETWORK=local

# OPTIONAL: Dry-run mode (set to "true" to log commands without executing GPIO operations)
# Useful for testing without hardware
DRY_RUN=false

# OPTIONAL: Poll interval in seconds (default: 2)
# How often to check for new events from the backend
POLL_INTERVAL=2

# OPTIONAL: State file location (default: /tmp/rpi_event_runner_state.json)
# Used to track processed events and prevent duplicates
STATE_FILE=/tmp/rpi_event_runner_state.json

# OPTIONAL: Allowed command prefix for security (default: gpioset)
# Only commands starting with this prefix will be executed
ALLOWED_PREFIX=gpioset
